server:
  port: 8080

spring:
  application:
    name: ecommerce-platform
  datasource:
    # Filled in T4 (Sprint 1, Day 4)
    # url: jdbc:postgresql://localhost:5432/ecommerce
    # username:
    # password:

management:
  endpoints:
    web:
      exposure:
        include: health,info
