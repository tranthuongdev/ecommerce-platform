spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: false  # set true for debugging if test fails
  flyway:
    enabled: true
    locations: classpath:db/migration
